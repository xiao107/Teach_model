# Teach_model · DeepSeek 数据助手智能教学平台
# 依赖清单（Windows / macOS 通用，Python 3.11+，建议 3.13）
# 版本与开发环境（conda teach_llm，macOS arm64）实测一致
#
# 安装：pip install -r requirements.txt

# --- Web 框架 ---
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32

# --- 数据与建模 ---
numpy==2.5.2
pandas==3.0.5
scikit-learn==1.9.0
tabulate==0.10.0        # pandas.DataFrame.to_markdown() 依赖
pydot==4.0.1            # 数据集 DOT 结构图解析（backend/data_loader.py）

# --- LLM / HTTP ---
httpx==0.28.1

# --- 配置 ---
python-dotenv==1.2.3
pydantic==2.13.4
pydantic-settings==2.15.0

# --- 测试（可选，tests/ 下的回归测试也可直接用 python 运行） ---
pytest>=8.0
